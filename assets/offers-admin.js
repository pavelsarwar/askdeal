let offerEditor=null,offerEditingId=null,offerMerchantRecords=[],offerMallRecords=[];

async function showOffersCMS(){
  moduleCard.innerHTML=`<div class="deals-head"><div><h2 style="margin:0">Offer Posts</h2><div class="result-count">Create and publish offers like a WordPress post</div></div><button class="btn btn-primary" onclick="openOfferEditor()">+ Add New Offer</button></div><div id="offerRows">Loading...</div>`;
  const {data,error}=await db.from('deals').select('id,title,status,offer_type,start_at,end_at,featured,merchants(name),malls(name),categories(name)').order('created_at',{ascending:false}).limit(100);const host=document.getElementById('offerRows');if(error){host.innerHTML=`<p>${escapeHtmlOffer(error.message)}</p>`;return;}host.innerHTML=`<div style="overflow:auto"><table class="admin-table"><thead><tr><th>Title</th><th>Category</th><th>Merchant / Brand</th><th>Shopping Mall</th><th>Type</th><th>Status</th><th>Dates</th><th>Actions</th></tr></thead><tbody>${(data||[]).map(r=>`<tr><td><b>${escapeHtmlOffer(r.title)}</b>${r.featured?' <span class="best-badge">Homepage Featured</span>':''}</td><td>${escapeHtmlOffer(r.categories?.name||'')}</td><td>${escapeHtmlOffer(r.merchants?.name||'—')}</td><td>${escapeHtmlOffer(r.malls?.name||'—')}</td><td>${escapeHtmlOffer(prettyOfferType(r.offer_type))}</td><td>${r.status==='published'?'<span class="best-badge">Published</span>':'Draft'}</td><td>${fmtDate(r.start_at)} – ${fmtDate(r.end_at)}</td><td><button class="btn btn-light" onclick="editOffer('${r.id}')">Edit</button> <button class="btn btn-light" onclick="deleteOffer('${r.id}')">Delete</button></td></tr>`).join('')}</tbody></table></div>`;
}
async function openOfferEditor(record=null){
  offerEditingId=record?.id||null;
  const [cats,merchants,malls]=await Promise.all([
    db.from('categories').select('id,name').eq('active',true).order('name'),
    db.from('merchants').select('id,name').eq('active',true).order('name'),
    db.from('malls').select('id,name').eq('active',true).order('name')
  ]);
  offerMerchantRecords=merchants.data||[];offerMallRecords=malls.data||[];
  moduleCard.innerHTML=`<div class="deals-head"><div><h2 style="margin:0">${record?'Edit':'Add'} Offer</h2><div class="result-count">Write, format and publish an offer</div></div><button class="btn btn-light" onclick="showOffersCMS()">← Back</button></div><form id="offerForm" onsubmit="saveOffer(event)" class="offer-editor-layout"><div class="offer-main"><div class="field"><label>Title</label><input id="offerTitle" required value="${attr(record?.title||'')}" placeholder="e.g. AEON Weekend Grocery Sale"></div><div class="field"><label>Details</label><div id="offerRichEditor"></div><div class="hint">Use headings, bold, lists, links and the image button. Images are uploaded to Supabase Storage.</div></div><div class="field"><label>Featured Image</label><div style="display:flex;gap:10px;align-items:center;flex-wrap:wrap"><input id="offerFeaturedImage" value="${attr(record?.image_url||'')}" placeholder="Image URL" style="flex:1"><button type="button" class="btn btn-light" onclick="pickFeaturedImage()">Upload Image</button></div><input id="featuredFileInput" type="file" accept="image/*" style="display:none" onchange="uploadFeaturedImage(this.files[0])"><div id="featuredPreview" style="margin-top:10px"></div></div><div class="field"><label>Short Summary</label><textarea id="offerDescription" rows="4">${escapeHtmlOffer(record?.description||'')}</textarea></div></div><aside class="offer-side"><div class="card" style="padding:16px"><h3 style="margin-top:0">Publish</h3><div class="field"><label>Status</label><select id="offerStatus"><option value="draft" ${selected(record?.status,'draft')}>Draft</option><option value="published" ${selected(record?.status,'published')}>Published</option></select></div><label style="display:flex;gap:8px;align-items:flex-start;margin:12px 0"><input id="offerFeatured" type="checkbox" ${record?.featured?'checked':''}><span><b>Show on Homepage / Featured</b></span></label><button class="btn btn-primary" style="width:100%">${record?'Update':'Save'} Offer</button></div><div class="card" style="padding:16px;margin-top:14px"><h3 style="margin-top:0">Offer Settings</h3><div class="field"><label>Category</label><select id="offerCategory"><option value="">Select category</option>${(cats.data||[]).map(x=>`<option value="${x.id}" ${String(record?.category_id||'')===String(x.id)?'selected':''}>${escapeHtmlOffer(x.name)}</option>`).join('')}</select><button type="button" class="btn btn-light" style="margin-top:8px;width:100%" onclick="quickAddOfferCategory()">+ Add New Category</button></div><div class="field"><label>Offer Type</label><select id="offerType"><option value="sale" ${selected(record?.offer_type,'sale')}>Sale / Offer</option><option value="warehouse" ${selected(record?.offer_type,'warehouse')}>Warehouse Sale</option><option value="clearance" ${selected(record?.offer_type,'clearance')}>Clearance</option><option value="promotion" ${selected(record?.offer_type,'promotion')}>Promotion</option><option value="freebie" ${selected(record?.offer_type,'freebie')}>Freebie</option></select></div><div class="field"><label>Merchant / Shop / Brand</label><select id="offerMerchant"><option value="">Select Merchant / Shop / Brand</option>${offerMerchantRecords.map(x=>`<option value="${x.id}" ${String(record?.merchant_id||'')===String(x.id)?'selected':''}>${escapeHtmlOffer(x.name)}</option>`).join('')}</select></div><div class="field"><label>Shopping Mall</label><select id="offerMall"><option value="">Select Shopping Mall</option>${offerMallRecords.map(x=>`<option value="${x.id}" ${String(record?.mall_id||'')===String(x.id)?'selected':''}>${escapeHtmlOffer(x.name)}</option>`).join('')}</select>${malls.error?`<div class="hint" style="color:#b42318">Shopping Mall database is not ready yet. Run supabase/malls.sql in Supabase.</div>`:''}</div><div class="field"><label>Discount Label</label><input id="offerDiscount" value="${attr(record?.discount_text||'')}" placeholder="e.g. Up to 50% OFF"></div><div class="field"><label>Start Date & Time</label><input id="offerStart" type="datetime-local" value="${toLocalInput(record?.start_at)}"></div><div class="field"><label>End Date & Time</label><input id="offerEnd" type="datetime-local" value="${toLocalInput(record?.end_at)}"></div><div class="field"><label>Source / Official Link</label><input id="offerSource" type="url" value="${attr(record?.source_url||'')}"></div></div></aside></form><div id="offerSaveMsg" class="hint"></div>`;
  offerEditor=new Quill('#offerRichEditor',{theme:'snow',placeholder:'Write full offer details here...',modules:{toolbar:{container:[[{'header':[1,2,3,false]}],['bold','italic','underline','strike'],[{'list':'ordered'},{'list':'bullet'}],[{'align':[]}],['blockquote','link','image'],['clean']],handlers:{image:quillImageHandler}}}});if(record?.content_html)offerEditor.clipboard.dangerouslyPasteHTML(record.content_html);renderFeaturedPreview();
}
async function editOffer(id){const {data,error}=await db.from('deals').select('*').eq('id',id).single();if(error)return alert(error.message);openOfferEditor(data)}
async function saveOffer(e){
  e.preventDefault();
  const title=document.getElementById('offerTitle').value.trim();
  if(!title)return setMsg('offerSaveMsg','Please enter an offer title.');
  const obj={
    title,
    slug:offerSlug(title)+'-'+Date.now().toString().slice(-6),
    category_id:document.getElementById('offerCategory')?.value||null,
    merchant_id:document.getElementById('offerMerchant')?.value||null,
    mall_id:document.getElementById('offerMall')?.value||null,
    offer_type:document.getElementById('offerType').value,
    status:document.getElementById('offerStatus').value,
    featured:document.getElementById('offerFeatured').checked,
    image_url:valueOrNull('offerFeaturedImage'),
    description:valueOrNull('offerDescription'),
    content_html:offerEditor.root.innerHTML,
    discount_text:valueOrNull('offerDiscount'),
    start_at:dateOrNull('offerStart'),
    end_at:dateOrNull('offerEnd'),
    source_url:valueOrNull('offerSource')
  };
  let result;if(offerEditingId){delete obj.slug;result=await db.from('deals').update(obj).eq('id',offerEditingId)}else result=await db.from('deals').insert(obj);if(result.error)return setMsg('offerSaveMsg',result.error.message);showOffersCMS();
}
async function deleteOffer(id){if(!confirm('Delete this offer?'))return;const {error}=await db.from('deals').delete().eq('id',id);if(error)alert(error.message);else showOffersCMS()}function pickFeaturedImage(){document.getElementById('featuredFileInput').click()}async function uploadFeaturedImage(file){if(!file)return;const url=await uploadOfferImage(file);if(url){document.getElementById('offerFeaturedImage').value=url;renderFeaturedPreview()}}function renderFeaturedPreview(){const box=document.getElementById('featuredPreview');const url=document.getElementById('offerFeaturedImage')?.value;if(box)box.innerHTML=url?`<img src="${attr(url)}" style="max-width:280px;max-height:180px;border-radius:12px;border:1px solid #e5e9f0">`:''}async function quillImageHandler(){const input=document.createElement('input');input.type='file';input.accept='image/*';input.onchange=async()=>{const file=input.files?.[0];if(!file)return;const url=await uploadOfferImage(file);if(url){const range=offerEditor.getSelection(true);offerEditor.insertEmbed(range.index,'image',url);offerEditor.setSelection(range.index+1)}};input.click()}async function uploadOfferImage(file){if(file.size>10*1024*1024){alert('Image must be below 10MB.');return null;}const ext=(file.name.split('.').pop()||'jpg').toLowerCase();const path=`offers/${new Date().getFullYear()}/${Date.now()}-${Math.random().toString(36).slice(2)}.${ext}`;const {error}=await db.storage.from('offer-media').upload(path,file,{cacheControl:'3600',upsert:false});if(error){alert(error.message);return null}return db.storage.from('offer-media').getPublicUrl(path).data.publicUrl;}function prettyOfferType(v){return ({sale:'Sale / Offer',warehouse:'Warehouse Sale',clearance:'Clearance',promotion:'Promotion',freebie:'Freebie'})[v]||v||''}function fmtDate(v){return v?new Date(v).toLocaleDateString('en-MY'):'—'}function selected(v,x){return (v||'')===x?'selected':''}function valueOrNull(id){const v=document.getElementById(id)?.value?.trim();return v||null}function dateOrNull(id){const v=document.getElementById(id)?.value;return v?new Date(v).toISOString():null}function toLocalInput(v){if(!v)return'';const d=new Date(v);const z=n=>String(n).padStart(2,'0');return `${d.getFullYear()}-${z(d.getMonth()+1)}-${z(d.getDate())}T${z(d.getHours())}:${z(d.getMinutes())}`}function offerSlug(v){return String(v||'').toLowerCase().normalize('NFKD').replace(/[\u0300-\u036f]/g,'').replace(/[^a-z0-9]+/g,'-').replace(/^-+|-+$/g,'')||'offer'}function attr(v){return escapeHtmlOffer(v).replace(/`/g,'&#96;')}function escapeHtmlOffer(v){return String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]))}