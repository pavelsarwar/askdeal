(async function(){const host=document.getElementById('promoCodeGrid');if(!host||!window.NAPS_SUPABASE||typeof supabase==='undefined')return;const client=supabase.createClient(window.NAPS_SUPABASE.url,window.NAPS_SUPABASE.anonKey);const now=new Date().toISOString();const {data,error}=await client.from('promo_codes').select('*').eq('status','published').or(`end_at.is.null,end_at.gte.${now}`).order('featured',{ascending:false}).order('end_at',{ascending:true});if(error){host.innerHTML='<div class="card"><h3>Promo codes are being updated</h3><p>Please check again shortly.</p></div>';return}const rows=data||[];host.innerHTML=rows.length?rows.map(r=>`<article class="card" style="position:relative"><div style="display:flex;justify-content:space-between;gap:10px;align-items:start"><div><div class="tag">${esc(r.merchant_name)}</div><h3 style="margin:8px 0">${esc(r.title)}</h3></div>${r.featured?'<span class="badge">FEATURED</span>':''}</div><div style="display:flex;align-items:center;justify-content:space-between;gap:12px;background:#f8fafc;border:1px dashed #98a2b3;border-radius:12px;padding:12px 14px;margin:14px 0"><strong style="font-size:20px;letter-spacing:1px">${esc(r.code)}</strong><button class="btn btn-primary" onclick="copyPromo('${js(r.code)}')">Copy Code</button></div>${r.discount_text?`<p><b>${esc(r.discount_text)}</b></p>`:''}${r.minimum_spend!=null?`<p style="font-size:13px;color:#667085">Minimum spend: RM ${Number(r.minimum_spend).toFixed(2)}</p>`:''}${r.end_at?`<p style="font-size:13px;color:#667085">Valid until ${new Date(r.end_at).toLocaleString('en-MY',{dateStyle:'medium',timeStyle:'short'})}</p>`:''}<details style="margin-top:12px"><summary style="cursor:pointer;font-weight:700">Terms & Conditions</summary><div style="margin-top:10px;line-height:1.6">${r.terms_html||'<p>No additional terms provided.</p>'}</div></details>${r.source_url?`<a class="btn btn-light" style="margin-top:14px" href="${esc(r.source_url)}" target="_blank" rel="noopener">Official / Redeem Link</a>`:''}</article>`).join(''):'<div class="card" style="grid-column:1/-1;text-align:center"><h3>No active promo codes right now</h3><p>New voucher codes will appear here when published.</p></div>';function esc(v){return String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]))}function js(v){return String(v??'').replace(/[\\']/g,'')}})();function copyPromo(code){navigator.clipboard?.writeText(code).then(()=>showToast('Promo code copied: '+code)).catch(()=>showToast('Code: '+code))}